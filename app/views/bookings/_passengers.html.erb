<% @booking.passengers.each do |passenger| %>
    <br />
    <div class="row mx-auto">
        <div class="col-sm-6">
            <strong>Name: <%= passenger.name %></strong>
        </div>
        <div class="col-sm-6">
            <strong>Email: <%= passenger.email %></strong>

        </div>

    </div>
<% end %>